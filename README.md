A reinforcement learning playground built around the board game Avalam. Currently, only a naive player is implemented which plays for red.

To play, run `python avalam.py` and open the `frontend/index.html` in your browser. 
